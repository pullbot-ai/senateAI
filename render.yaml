services:
  - type: web
    name: senate-ai
    runtime: python
    buildCommand: pip install flask requests gunicorn torch
    startCommand: gunicorn backend_render:app --bind 0.0.0.0:$PORT --timeout 120
    
